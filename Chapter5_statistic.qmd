---
title: "Chapter 5 통계"
author: "Rchemist"
format: html
editor: visual
---

## 1. 집단 간 차이 검정

### 1) T-test:

### 2) ANOVA:

### 3) $\chi2$ (Chi-square) test

------------------------------------------------------------------------

## 2. 상관 분석

------------------------------------------------------------------------

## 3. 회귀 분석

### 1) 선형 회귀

### 2) 로지스틱 회귀분석

------------------------------------------------------------------------

## 4. 셀프 과제

```{r}
dt5 <- fread('csv/5주차_diabetes.csv')
dt5_2 <- fread('csv/5주차_insurance.csv')

dt5_2 %>% str()
```

### 1) 데이터 준비하기

1-1. 5주차 셀프 과제 데이터 두 건을 불러옵니다.

-   당뇨 데이터: 5주차_diabetes.csv

-   보험료 데이터: 5주차_insurance.csv

```{r}

```

------------------------------------------------------------------------

### 2) 집단 간 분석

2-1. 보험료 데이터에서 성별에 따른 BMI의 평균 차이가 유의한지 확인하고 이를 해석하세요.

```{r}

```

2-2. 당뇨 데이터에서 당뇨 여부 (Outcome)에 따른 Glucose의 평균 차이가 유의한지 확인하고 이를 해석하세요.

```{r}

```

2-3. 보험료 데이터에서 지역 (region)에 따른 보험료의 평균 차이가 유의한지 확인하고 이를 해석하세요. 만약 차이가 유의하면, 어떤 집단 간 차이가 유의한지 확인하세요.

```{r}

```

2-4. 당뇨 데이터에서 Age column을 활용하여 연령대 column을 만든 뒤, 연령대에 따른 Glucose의 평균 차이가 유의한지 확인하고 이를 해석하세요. 만약 차이가 유의하면, 어떤 집단 간 차이가 유의한지 확인하세요.

```{r}

```

2-5. 보험료 데이터에서 지역 (region) 에 따른 성별 (sex)의 응답 분포의 차이가 유의한지 확인하고 이를 해석하세요. 만약 차이가 유의하면, 어떤 집단 간 차이가 유의한지 확인하세요.

```{r}

```

### 3) 상관 분석

3-1. 보험료 데이터에서 보험료와 유의미한 상관 관계를 갖는 변수를 찾으세요.

```{r}
```

3-2. 당뇨 데이터에서 glucose, 혈당, 인슐린, BMI, 나이의 상관 계수를 확인한 뒤. 통계적 유의성을 파악하세요.

```{r}

```

### 4) 선형 회귀 분석

4-1. 보험료 데이터에서 단변량 선형 회귀 분석을 통해 보험료에 유의한 영향을 미치는 변수를 찾으세요.

```{r}

```

4-2. 보험료 데이터에서 보험료에 유의한 영향을 미치는 변수들을 모두 넣어, 다변량 선형 회귀 분석을 실시하고 결과를 해석하세요.

```{r}

```

4-3. stepwise 회귀분석을 통해 최적의 회귀분석 모델을 구한 뒤, 결과를 해석하세요.

```{r}

```

------------------------------------------------------------------------

### 5) 로지스틱 회귀 분석

5-1. 당뇨 데이터에서 단변량 로지스틱 회귀 분석을 통해 당뇨일 확률을 유의하게 높이는 변수들을 모두 찾으세요.

```{r}

```

5-2. 당뇨 데이터에서 당뇨일 확률을 유의하게 높이는 변수들을 모두 넣어, 다변량 로지스틱 회귀 분석을 실시하고 결과를 해석하세요.

```{r}
glm(Outcome ~ Pregnancies + Glucose + BMI + Age, family='binomial',data=dt5) %>% summary
```

5-3. stepwise 회귀분석을 통해 최적의 회귀분석 모델을 구한 뒤, 결과를 해석하세요.

```{r}

```

```{r}
fit <- lm(charges ~ age + sex + bmi + children + smoker + region, dt5_2)
```

### 2) Logistic regression

```{r}

```
